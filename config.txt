[Config]
# Experiment-facing recruitment, payment, and contact settings are defined in
# Exp.config in experiment.py.
host = 0.0.0.0

# Required for PsyNet/Dallinger Docker SSH launches.
# docker_image_base_name = ghcr.io/lucasgautheron/music-rating/psynet-experiment-images
# docker_volumes = ${HOME}/psynet-data/assets:/psynet-data/assets

